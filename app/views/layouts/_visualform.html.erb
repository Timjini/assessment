<%= form_for Visual.new do |form|%>
    <div class="mx-auto pb-5">
    <%= form.file_field :image %>
    <%= form.submit "Upload", class: "mx-1 rounded-md bg-blue-800 px-2 py-1 text-gray-100 hover:text-white focus:outline-none focus:ring-2 font-medium focus:ring-white focus:ring-offset-2 focus:ring-offset-gray-800" %>
    </div>

<% end %>
